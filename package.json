{
  "name": "scalar-set-theory",
  "scripts": {
    "start": "elm-reactor",
    "build": "elm-make src/ScalarSetTheory/Main.elm --output dist/index.html",
    "deploy": "cf push scalar-set-theory -b https://github.com/cloudfoundry/staticfile-buildpack.git -m 64m",
    "test": "elm-test && elm-reactor -p 8001 & cd integration-tests && npm test"
  }
}
